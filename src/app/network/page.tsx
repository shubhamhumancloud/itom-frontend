import { AppShell } from '@/components/app/app-shell';
import { NetworkOverviewPage } from '@/components/itom/pages/network-overview-page';

export default function NetworkPage() {
  return (
    <AppShell>
      <NetworkOverviewPage />
    </AppShell>
  );
}
