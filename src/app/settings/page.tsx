import { AppShell } from '@/components/app/app-shell';
import { SettingsPageContent } from '@/components/itom/pages/settings-page-content';

export default function SettingsPage() {
  return (
    <AppShell>
      <SettingsPageContent />
    </AppShell>
  );
}
