import { AppShell } from '@/components/app/app-shell';
import { ProfilePageContent } from '@/components/itom/pages/profile-page-content';

export default function ProfilePage() {
  return (
    <AppShell>
      <ProfilePageContent />
    </AppShell>
  );
}
