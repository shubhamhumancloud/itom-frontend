import { AppShell } from '@/components/app/app-shell';
import { TopologyPage } from '@/components/itom/pages/topology-page';

export default function DiscoveryTopologyRoute() {
  return (
    <AppShell>
      <TopologyPage />
    </AppShell>
  );
}
