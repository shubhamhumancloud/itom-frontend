import { AppShell } from '@/components/app/app-shell';
import { FleetMetricsPage } from '@/components/itom/pages/fleet-metrics-page';

export default function MetricsPage() {
  return (
    <AppShell>
      <FleetMetricsPage />
    </AppShell>
  );
}
